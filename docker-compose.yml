version: '3.8'

services:
  mask-detector:
    build: .
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./reports:/app/reports
    environment:
      - DATA_DIR=/app/data
      - MODEL_DIR=/app/models
      - REPORT_DIR=/app/reports
    working_dir: /app
